{
  "name": "asesoriasexcel-root",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Asesorias Excel - Proyecto monorepo. Frontend en /client, Vercel Functions en /api.",
  "scripts": {
    "start-dev": "npx vercel dev --listen 3000"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "googleapis": "^140.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
