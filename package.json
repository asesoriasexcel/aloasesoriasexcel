{
  "name": "asesoriasexcel-root",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Asesorias Excel - Proyecto monorepo. Frontend en /client, Vercel Functions en /api.",
  "scripts": {
    "start-dev": "concurrently \"npx vercel dev --listen 3001\" \"npm run dev --prefix client -- --port 3000\""
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "googleapis": "^140.0.0",
    "mercadopago": "^2.12.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
